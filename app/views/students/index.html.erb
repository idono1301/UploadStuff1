<h1>Listing students</h1>

<table>
  <tr>
	<th><%= link_to 'Name', students_path(:sort_by => "name") %></th>
	<th><%= link_to 'ID', students_path(:sort_by => "number") %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @students.each do |student| %>
  <tr>
    <td><%= student.name %></td>
    <td><%= student.number %></td>
    <td><%= link_to 'Show', student %></td>
    <td><%= link_to 'Edit', edit_student_path(student) %></td>
    <td><%= link_to 'Destroy', student, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Update Students', students_path(:update => "students") %>
